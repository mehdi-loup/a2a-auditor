# Copy to .env and fill in. Never commit .env.
# Base mainnet RPC, read-only. Public default works for light use;
# use a provider URL (Alchemy/Infura) for heavy log scans.
BASE_RPC_URL=https://mainnet.base.org
# Required for Phase 2 (LLM judge). Not needed for Phase 0/1.
ANTHROPIC_API_KEY=
